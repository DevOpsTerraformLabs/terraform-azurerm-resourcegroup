# terraform-azurerm-resourcegroup
**Single-purpose Terraform module (module at repo root)** that create an Azure Resource Group.

This repository contains only the module implementation (at the root) and a minimal example in `example/simple/`. Consumers can all the module directly from the repository root via GitHub URL and pin a tag with `?ref=vX.Y.Z`.

---